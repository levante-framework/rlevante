# Get metadata for mapping items

Get metadata for mapping items

## Usage

``` r
fetch_mapping_items()
```

## Examples

``` r
if (FALSE) { # \dontrun{
mapping_items <- fetch_mapping_items()
} # }
```
