# Get participants

Get participants

## Usage

``` r
build_filter(var, vals)
```

## Examples

``` r
if (FALSE) { # \dontrun{
dataset_spec <- list(list(name = "levante-example-dataset:bm7r", version = "current"))
participants <- get_participants(dataset_spec)
} # }
```
