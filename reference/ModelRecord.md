# constructor helper

constructor helper

helper to extract and tidy scores

method for show generic

method for AIC generic

method for BIC generic

method for logLik generic

## Usage

``` r
modelrecord(mod, row_ids)

extract_scores(mod, row_ids)

# S4 method for class 'ModelRecord'
show(object)

# S4 method for class 'ModelRecord'
AIC(object)

# S4 method for class 'ModelRecord'
BIC(object)

# S4 method for class 'ModelRecord'
logLik(object)

model_class(object)

model_vals(object)

items(object)

scores(object)

tabdata(object)
```

## Arguments

- mod:

  mirt model object

- row_ids:

  character vector

- object:

  ModelRecord object
