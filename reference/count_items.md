# count number of items with non-NA responses for each run

count number of items with non-NA responses for each run

## Usage

``` r
count_items(mod_rec)
```

## Arguments

- mod_rec:

  ModelRecord object
