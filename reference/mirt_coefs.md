# get item parameters of fitted mirt model

get item parameters of fitted mirt model

## Usage

``` r
mirt_coefs(mod, IRTpars = TRUE)
```

## Arguments

- mod:

  fit mirt model (SingleGroupClass or MultiGroupClass object)

- IRTpars:

  logical; convert slope intercept parameters into traditional IRT
  parameters?
